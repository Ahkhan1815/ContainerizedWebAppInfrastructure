events {}

http {
    server {
        listen 80;

        location /api/ {
            proxy_pass http://backend:8080/;
            proxy_set_header Host $host; 
            #preserves original hostname
            proxy_set_header X-Real-IP $remote_addr; 
            #sends real client IP to backend
        }
    }
}
